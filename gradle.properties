# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx6G
org.gradle.parallel=false
org.gradle.caching=true
org.gradle.caching.debug=false
#org.gradle.configureondemand=true

# Mod properties
mod.version=9.9
mod.changelog=\
- Port to 1.21.9
mod.api_version=1.2
mod.group=mod.crend
mod.id=dynamiccrosshair
mod.name=Dynamic Crosshair

# Used for the mod metadata
mod.mc_dep=[VERSIONED]
# Used for the release title. I.e. '1.20.x'
mod.mc_title=[VERSIONED]
# Space separated versions for publishing. I.e. '1.20, 1.20.1'
mod.mc_targets=[VERSIONED]

mod.include_datagen=false

# Mod setup
deps.mixin_extras=0.4.1
deps.fabric_loader=0.17.0
deps.forge_loader=[VERSIONED]
deps.neoforge_loader=[VERSIONED]
deps.neoforge_patch=[VERSIONED]
deps.forge_patch=[VERSIONED]

# Mod dependencies
deps.yarn_build=[VERSIONED]
deps.fabric_api=[VERSIONED]
deps.libbamboo=[VERSIONED]
deps.yacl=[VERSIONED]
deps.autohud=[VERSIONED]

deps.modmenu=[VERSIONED]
deps.mythicmounts=[VERSIONED]

# Publishing
publish.dryrun=false
publish.current=true
publish.modrinth=ZcR9weSm
publish.curseforge=623800
publish.curseforge_slug=dynamiccrosshair
